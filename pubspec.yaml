name: scalpex2
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.3

# Add regular dependencies here.
dependencies:
  hyperliquid_dart_sdk:
    git:
      url: https://github.com/stringeee/hyperliquid_dart_sdk
      ref: main  # или tag/commit
  path: ^1.9.0
  web_socket_channel: ^3.0.1
  dio: ^5.0.0
  http: ^1.6.0
  injectable: ^1.5.3
  get_it: ^7.2.0

dev_dependencies:
  injectable_generator:
  build_runner: ^2.3.3
  lints: ^6.0.0
  test: ^1.25.6
